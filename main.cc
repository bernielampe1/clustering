#include "cluster.h"

int main(int argc, char **argv)
{
    // proc args
    // input image
    // create vectors
    // cluster vectors
    // render vectors into new image
    // write output color image with clusters

    return 0;
}
